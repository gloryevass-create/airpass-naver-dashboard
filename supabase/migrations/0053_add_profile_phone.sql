-- ============================================================================
-- profiles에 phone(핸드폰번호) 추가(2026-08-30) — google_email(0048)과 같은
-- 방식: 관리자 등록 폼에서 처음 입력하거나, 본인이 회원정보 수정 화면에서
-- 직접 채운다. 형식 검증은 따로 두지 않는다(자유 텍스트, title과 동일).
-- ============================================================================

alter table public.profiles add column if not exists phone text;
